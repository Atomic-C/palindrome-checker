A program made to verify if a word is palindrome using Java. A Palindrome is a word or phrase which can be read the same way from left to right and vice versa. IE: RADAR, MADAM and POOP.

palindrome-checker features:

Verifies if a word is palindrome via user input;
TDD;
